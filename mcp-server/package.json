{
  "name": "hd2-wiki-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Manual-trigger MCP server that fetches Helldivers 2 enemy/weapon stats from helldivers.wiki.gg and caches them locally.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "dev": "tsc -p . && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
